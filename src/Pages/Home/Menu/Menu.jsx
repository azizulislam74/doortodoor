import ShareTitle from "../../../Components/ShareTitle/ShareTitle";

const Menu = () => {
    return (
        <div>
            <ShareTitle subHeader="Check Out Plz" header="ORDER"></ShareTitle>
        </div>
    );
};

export default Menu;